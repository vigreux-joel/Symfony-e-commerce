{% extends 'base.html.twig' %}

{% block title %}{{product.name}}- La boutique Francaise{% endblock %}

{% block content %}

    <div class="row">
        <div class="col-md-5">
            <img src="/uploads/images/{{ product.illustration }}" alt="{{ product.name }}" class="img-fluid">
        </div>
        <div class="col-md-7 my-auto">
            <h3>{{ product.name }}</h3>
            <p>{{ product.subtitle }}</p>
            <span class="product-page-price">{{ (product.price/100)|format_currency('EUR') }} €</span>
            <hr>
            <p>{{ product.description }}</p>
            {{ form(form) }}
            <!--<a href="{{ path('cart_add', {'id': product.id}) }}" class="btn btn-primary">Ajouteur au panier</a>-->
        </div>
    </div>

{% endblock %}
